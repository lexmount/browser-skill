# <Topic Name> — Design

**Status:** draft | active | superseded-by-<path>
**Last updated:** <YYYY-MM-DD> from commit `<sha>` / plan `<slug>`

## 问题背景

说明这个长期主题为什么存在。必须写清：

- 用户 / 系统要达成的真实目标。
- 过去方案为什么不够。
- 当前设计要守住的非功能约束，例如安全、可复现、可移植、人工审批。

## 设计原则

- <原则 1：如何裁决实现取舍>
- <原则 2：什么证据才算有效>
- <原则 3：哪些捷径禁止使用>

## 架构

完整描述最新方案，不能只写“见 spec / plan”。建议覆盖：

- 上游输入和触发条件。
- 核心模块 / skill / 服务边界。
- 外部系统和鉴权方式。
- 持久化位置与 git 跟踪策略。
- 本地开发态和生产态差异。

## 组件

| 组件 | 职责 | 输入 | 输出 | 失败模式 |
|------|------|------|------|----------|
| `<component>` | <做什么> | <数据 / 调用> | <产物> | <如何失败 / 如何恢复> |

## 数据流 / 控制流

用编号步骤写真实流转：

1. <入口>
2. <校验 / 权限 / 配置读取>
3. <核心执行>
4. <产物生成>
5. <验证 / 反馈 / 状态沉淀>

## 错误处理与降级

- **用户可修复错误：** <提示什么、在哪里修>
- **环境错误：** <服务未启动、凭证缺失、网络不通>
- **协议 / 数据形状错误：** <如何 fail fast>
- **不可接受降级：** <禁止悄悄 fallback 的路径>

## 验收证据

| 验收项 | 命令 / 证据 | 通过标准 |
|--------|-------------|----------|
| <行为> | `<command>` | <输出 / artifact / review 结论> |

## 开放问题

- <还未定的设计问题，写明阻塞程度和下一步>

## Provenance

- YYYY-MM-DD：<本次同步来源> — plan `<slug>` / commit `<sha>` / PR #N
